# wagmi 3 and the Openfort SDK are newer than the default minimum-release-age
# window; allow just these packages through.
minimumReleaseAgeExclude[]=wagmi
minimumReleaseAgeExclude[]=@openfort/react
minimumReleaseAgeExclude[]=@openfort/openfort-js
